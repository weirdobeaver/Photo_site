<%= simple_form_for @contact, :html => { :class => 'form-vertical' } do |f| %>
  <%= f.input :name, label: 'Imię i nazwisko' %>
  <%= f.input :email %>
  <%= f.input :message, label: 'Treść wiadomości', :as => :text, :input_html => { :cols => 50, :rows => 9 } %>
 	<%= f.submit 'Wyślij' %>
<% end %>